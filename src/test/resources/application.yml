logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              ReactiveMongoTemplate: debug

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
