

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              ReactiveMongoTemplate: debug
spring:
  rsocket:
    server:
      port: 7000
      transport: tcp
